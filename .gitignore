# -------------------------------------------------------------
# Project-specific ignore rules for ~/bin/bash
# -------------------------------------------------------------
# A shared ~/.gitignore_global now filters out everyday clutter
# (editor state, caches, virtualenvs, archives, etc.). Keep this
# repo-level file focused on items that are unique to the shell
# scripts toolkit so future additions stay obvious.
# -------------------------------------------------------------

# Local archives created by rotation/packaging scripts.
archive/
